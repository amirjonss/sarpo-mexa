# Telegram analytics — copy to .env.local and fill in real values.
# .env.local is git-ignored (*.local), so credentials stay out of the repo.

# Bot token from @BotFather
VITE_TG_BOT_TOKEN=

# Target chat: your user id, a group id, or a channel id (e.g. -1001234567890)
VITE_TG_CHAT_ID=
